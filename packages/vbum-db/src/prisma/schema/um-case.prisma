model um_case {
        id              Int       @id @default(autoincrement()) @db.UnsignedInt
        pub_id          String    @unique @db.VarChar(12)
        created_at      DateTime  @db.DateTime(0)
        created_by      String    @db.VarChar(50)
        updated_at      DateTime  @db.DateTime(0)
        updated_by      String    @db.VarChar(50)
        assigned_at     DateTime? @db.DateTime(0)
        assigned_to     String?   @db.VarChar(50)
        client_pub_id   String    @db.VarChar(12)
        plan_pub_id     String    @db.VarChar(12)
        status          String    @db.VarChar(255)
        case_number     String    @unique @db.VarChar(50)
        case_type       String    @db.VarChar(50)
        recd_date       DateTime  @db.Date
        procedure_codes String?   @db.VarChar(10000)
        md_review       Boolean   @default(false)
        phys_pub_id     String?   @db.VarChar(12)
        fu_action       String?   @db.VarChar(255)
        md_recommended  String?   @db.VarChar(255)
        p2p_success     Boolean   @default(false)
        closed_at       DateTime? @db.Date
        remarks         String?   @db.VarChar(1000)

        @@index([client_pub_id])
        @@index([plan_pub_id])
        @@index([assigned_to])
}

model um_case_hist {
        id              Int       @id @default(autoincrement()) @db.UnsignedInt
        hist_added_at   DateTime  @db.DateTime(0)
        pub_id          String    @db.VarChar(12)
        created_at      DateTime  @db.DateTime(0)
        created_by      String    @db.VarChar(50)
        updated_at      DateTime  @db.DateTime(0)
        updated_by      String    @db.VarChar(50)
        assigned_at     DateTime? @db.DateTime(0)
        assigned_to     String?   @db.VarChar(50)
        client_pub_id   String    @db.VarChar(12)
        plan_pub_id     String    @db.VarChar(12)
        status          String    @db.VarChar(255)
        case_type       String    @db.VarChar(50)
        recd_date       DateTime  @db.Date
        case_number     String    @db.VarChar(50)
        procedure_codes String?   @db.VarChar(10000)
        md_review       Boolean   @default(false)
        phys_pub_id     String?   @db.VarChar(12)
        fu_action       String?   @db.VarChar(255)
        p2p_success     Boolean   @default(false)
        md_recommended  String?   @db.VarChar(255)
        closed_at       DateTime? @db.Date()
        remarks         String?   @db.VarChar(1000)

        @@index([pub_id])
        @@index([client_pub_id])
        @@index([plan_pub_id])
        @@index([assigned_to])
        @@index([case_number])
}
